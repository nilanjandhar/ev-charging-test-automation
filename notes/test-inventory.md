# Test inventory

62 test functions. Parametrized ones expand further at collection time —
`pytest --collect-only -q` reports 109 collected tests in total.

Generated by `make inventory`; do not edit by hand. Tier definitions are in
`TEST_STRATEGY.md`.

| Tier | Tests |
|---|---|
| **P0** | 21 |
| **P1** | 27 |
| **P2** | 14 |

## `tests/unit/test_scoring_boundaries.py`

5 tests.

| Tier | Test | Covers |
|---|---|---|
| P0 | `test_score_at_each_threshold` | A change to any scoring constant, divisor or cap must go red here |
| P0 | `test_flagging_boundary_is_exclusive` | The flag test is strict `<`, so 60.0 exactly is *not* flagged |
| P0 | `test_dead_station_reporting_clean_metrics_is_not_flagged` | An offline station with no errors and no latency scores exactly 60.0 |
| P1 | `test_error_penalty_saturates_so_catastrophe_is_indistinguishable` | 6 errors and 100,000 errors produce the same score, and neither is flagged |
| P2 | `test_score_floor_is_ten_making_the_zero_clamp_unreachable` | Dead code: the documented range is [0, 100], the reachable range is [10, 100] |

## `tests/unit/test_scoring_properties.py`

11 tests.

| Tier | Test | Covers |
|---|---|---|
| P0 | `test_score_stays_inside_its_reachable_range` | No input can push the score outside [10, 100] |
| P0 | `test_more_errors_never_improves_the_score` | Monotonicity in error_count — non-increasing, not strictly decreasing |
| P0 | `test_more_latency_never_improves_the_score` | Monotonicity in latency_ms — a slower station is never scored healthier |
| P1 | `test_score_is_blind_to_error_count_above_the_cap` | Above 6 errors the score carries no information about error volume |
| P0 | `test_going_offline_costs_forty_points_give_or_take_the_rounding` | The offline penalty is independent of the other two terms — to within 0.01 |
| P0 | `test_an_online_station_with_no_errors_can_never_be_flagged` | Latency alone is never enough to flag a station, at any value |
| P0 | `test_flag_agrees_with_the_threshold_for_every_input` | `flagged` is exactly `score < FLAGGING_THRESHOLD`, with no drift |
| P0 | `test_scoring_is_pure` | Identical inputs always produce identical outputs |
| P1 | `test_flagging_requires_more_than_connectivity_loss_alone` | What does it take to flag an offline station? |
| P1 | `test_offline_station_with_sub_threshold_latency_rounds_back_to_unflagged` | The offline blind spot is an interval, not a single point |
| P1 | `test_rounding_step_can_decide_the_flag` | At the boundary the last 0.01 is settled by binary float rounding |

## `tests/contract/test_openapi_conformance.py`

7 tests.

| Tier | Test | Covers |
|---|---|---|
| P1 | `test_the_service_publishes_a_valid_openapi_document` | The schema itself must be well-formed 3.1 and cover every documented endpoint |
| P1 | `test_every_documented_success_response_matches_its_schema` | One flow, every endpoint, each response validated against its own contract |
| P1 | `test_validation_errors_match_the_documented_error_schema` | The 422 envelope is part of the contract and clients parse it |
| P1 | `test_metrics_summary_declares_a_nullable_average` | `average_latency_ms` must stay nullable in the published schema |
| P2 | `test_the_404_the_service_actually_returns_is_documented` | A status code the service returns must appear in the schema it publishes |
| P1 | `test_timestamps_conform_to_the_date_time_format_they_declare` | The service violates its own published `format: date-time` |
| P2 | `test_the_undocumented_404_body_is_at_least_consistent` | The shape clients have to reverse-engineer |

## `tests/contract/test_schemathesis_fuzz.py`

1 tests.

| Tier | Test | Covers |
|---|---|---|
| P1 | `test_operation_conforms_to_its_published_schema` | Every operation answers schema-valid inputs with schema-valid responses |

## `tests/api/test_cross_endpoint_consistency.py`

10 tests.

| Tier | Test | Covers |
|---|---|---|
| P0 | `test_a_single_report_is_reflected_identically_by_every_endpoint` | Ingest -> status -> list -> metrics must describe the same station |
| P0 | `test_flagged_stations_agree_across_list_worklist_and_metrics` | The poor-hygiene worklist is exactly the set of stations flagged elsewhere |
| P0 | `test_metrics_aggregate_only_the_latest_report_per_station` | Superseded reports must not leak into network totals |
| P1 | `test_metrics_on_an_empty_network` | The zero case has to be representable, not a division by zero |
| P2 | `test_station_listing_is_ordered_stably` | `/stations` is ordered by station_id and must stay that way |
| P1 | `test_one_absurd_latency_report_poisons_the_network_average` | `latency_ms` has no upper bound and the metric is an unweighted mean |
| P1 | `test_an_infinite_latency_report_erases_the_network_average_entirely` | One report of `Infinity` and the latency KPI silently becomes null |
| P0 | `test_a_retried_report_does_not_duplicate_the_station_in_the_listing` | `GET /stations` must list each known station exactly once |
| P0 | `test_a_retried_report_does_not_inflate_network_metrics` | One station that reports twice is still one station |
| P1 | `test_a_retried_report_leaves_the_station_detail_view_correct` | `/stations/{id}/status` is idempotent |

## `tests/api/test_ingest_validation.py`

11 tests.

| Tier | Test | Covers |
|---|---|---|
| P1 | `test_invalid_field_is_rejected_with_a_machine_readable_error` | A bad field must produce 422 naming that field — not a 500, not a silent accept |
| P1 | `test_every_field_is_required` | No field has a server-side default; omitting any one is a 422, not a partial record |
| P0 | `test_unknown_fields_are_ignored_and_cannot_override_the_computed_score` | A client cannot inject its own hygiene score. This is the one that matters |
| P2 | `test_numeric_strings_are_coerced_rather_than_rejected` | Pydantic's lax mode accepts `"120"` for a float. The brief implies it would not |
| P1 | `test_malformed_bodies_are_422_not_500` | Junk on the wire must never reach the database layer or produce a stack trace |
| P1 | `test_unknown_station_is_a_clean_404` | A station nobody has reported for must 404 with a usable message, not an empty 200 |
| P2 | `test_station_ids_are_matched_exactly` | Lookup is exact-match: no trimming, no case folding, no prefix matching |
| P2 | `test_a_large_but_plausible_payload_is_accepted` | Neither `station_id` nor `firmware_version` has an upper length bound |
| P1 | `test_an_enormous_error_count_is_rejected_rather_than_crashing_ingest` | Schema-valid input that reaches the storage layer and blows up there |
| P1 | `test_the_int64_boundary_below_the_crash_is_accepted` | The other side of that boundary, so the xfail above cannot drift |
| P2 | `test_wrong_method_and_missing_route_are_not_500s` | The edges of the routing table: 405 and 404, with JSON bodies |

## `tests/api/test_recency_semantics.py`

5 tests.

| Tier | Test | Covers |
|---|---|---|
| P0 | `test_status_reflects_the_newest_timestamp_not_the_newest_arrival` | Arrival order must not decide station state |
| P0 | `test_a_future_dated_report_permanently_masks_every_later_report` | One bad clock and the station is never heard from again |
| P1 | `test_utc_offsets_are_dropped_rather_than_normalised` | Two reports of the same instant are not treated as the same instant |
| P1 | `test_timestamps_lose_their_timezone_on_the_round_trip` | What goes in as UTC comes back with no zone at all |
| P1 | `test_reports_that_tie_on_timestamp_do_not_crash_the_detail_view` | A tie has to resolve to *something* deterministic per endpoint |

## `tests/e2e/test_live_service.py`

5 tests.

| Tier | Test | Covers |
|---|---|---|
| P0 | `test_health_endpoint_answers_over_real_http` | The readiness signal CI polls has to be exactly what CI expects |
| P0 | `test_a_station_journey_over_the_wire` | The whole operator journey against the real deployment, in deltas |
| P1 | `test_error_responses_serialise_correctly_over_http` | 422 and 404 survive real serialisation, with real content types |
| P2 | `test_the_deployment_serves_its_documentation_and_dashboard` | The static mount and the docs routes only exist in a real deployment |
| P2 | `test_unknown_paths_do_not_leak_a_stack_trace` | A 404 from the real server is a JSON body, not a debug page |

## `tests/perf/test_concurrent_writes.py`

4 tests.

| Tier | Test | Covers |
|---|---|---|
| P0 | `test_concurrent_reports_for_one_station_all_land` | N simultaneous writers, N stored reports, and the final state is one of them |
| P1 | `test_concurrent_writers_across_stations_leave_metrics_consistent` | An aggregate read taken after concurrent writes must not be internally torn |
| P2 | `test_repeated_identical_reads_are_stable` | A read with no intervening write must not change under repetition |
| P1 | `test_in_process_client_agrees_with_the_wire_on_a_single_report` | A control for the whole e2e layer: does in-process testing miss anything here? |

## `tests/perf/test_latency_smoke.py`

2 tests.

| Tier | Test | Covers |
|---|---|---|
| P2 | `test_read_and_write_latency_against_a_configured_budget` | Every documented endpoint answers within the configured p95 budget |
| P2 | `test_metrics_latency_does_not_degrade_with_report_volume` | Does the aggregate query blow up as history grows? |

## `tests/ui/test_dashboard_smoke.py`

1 tests.

| Tier | Test | Covers |
|---|---|---|
| P2 | `test_dashboard_renders_an_ingested_station` | A station ingested via the API appears on the dashboard with its real status |
